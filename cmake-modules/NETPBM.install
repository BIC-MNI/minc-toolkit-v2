#! /bin/sh

p=`pwd`

if [ -z $1 ];then
  echo Usage: $0 install_prefix
  exit 1
fi
install_prefix=$1

cp $p/pkg/link/libnetpbm.a ${install_prefix}/lib/
cp $p/pkg/include/*.h ${install_prefix}/include/